import { describe, it, expect } from 'vitest';

describe('vlossom', () => {
    it('should be defined', () => {
        // given
        const vlossom = 'vlossom';

        // when
        // then
        expect(vlossom).toBeDefined();
    });
});
