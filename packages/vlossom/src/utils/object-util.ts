import { assign, crush, get, isEmpty, isEqual, isObject, omit, shake } from 'radash';

export const objectUtil = {
    assign,
    crush,
    get,
    isEmpty,
    isEqual,
    isObject,
    omit,
    shake,
};
